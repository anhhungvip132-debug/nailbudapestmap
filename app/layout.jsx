export const metadata = {
  title: "Nail Budapest Map",
  description: "Find the best nail salons near you in Budapest"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
